# Sprint 36 Offscreen Screenshots

These images were rendered offscreen by `egui_kittest` with `wgpu` on the Mesa `llvmpipe`
software adapter inside a sandbox without a display server or GPU. They come from the production
renderer but not from a compositor; real-desktop captures are human-only item H1 in
[the human-only register](../sprint-36-human-only.md). The complete set and its manifests are
listed in [the verification record](../sprint-36-native-ui-verification.md).
